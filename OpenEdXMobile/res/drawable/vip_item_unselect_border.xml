<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 填充色 -->
    <solid android:color="@color/vip_item_unselect_border_solid" />

    <!-- 圆角 -->
    <corners
        android:bottomLeftRadius="@dimen/vip_item_radius"
        android:bottomRightRadius="@dimen/vip_item_radius"
        android:topLeftRadius="@dimen/vip_item_radius"
        android:topRightRadius="@dimen/vip_item_radius" />

    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/vip_item_unselect_border_stroke" />

</shape>